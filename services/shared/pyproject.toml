[project]
name = "shared"
version = "0.1.0"
description = "Shared modules for ai-document-platform"
requires-python = ">=3.10"

dependencies = [
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
